[package]
name = "gooseswitcher"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "IBus layout correction engine for Russian and English"

[features]
default = []
bundled-sqlite = ["rusqlite/bundled"]

[dependencies]
directories = "6"
rusqlite = "0.37"

[dev-dependencies]
tempfile = "3"
